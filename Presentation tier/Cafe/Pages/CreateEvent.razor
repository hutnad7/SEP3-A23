@page "/create-event"

@using Cafe.Models
@using System.Globalization
@inject NotificationService NotificationService

@inject HttpClient HttpClient

<h3>Create event</h3>

<RadzenTemplateForm TItem="EventPage" Data=EventPage Submit="@SubmitEvent">
    <RadzenTextBox @bind-Value="EventPage.Name" Name="Name" Placeholder="Event Name" Style="width:100%; margin-bottom: 20px;" />
    <RadzenTextBox @bind-Value="EventPage.Description" Name="Description" Placeholder="Description" Style="width:100%; margin-bottom: 20px;" />
    <div class="rz-p-sm-12 rz-text-align-center">
        <RadzenDropDown @bind-Value="EventPage.EntertainerId" Data=@entertainers TextProperty="FullName" ValueProperty="Id" Style="width: 100%; max-width: 400px;" />
    </div>
    
    <RadzenNumeric @bind-Value="EventPage.AvailablePlaces" />
    <!--  <RadzenTextBox @bind-Value="EventPage.DateString" Name="Date" Placeholder="Date" Style="width:100%; margin-bottom: 20px;" />
    -->
    <RadzenDatePicker @bind-Value="EventPage.Start" ShowTime="true" HourFormat="12" Culture='CultureInfo.CreateSpecificCulture("en-GB")' DateFormat="yyyy/MM/dd hh:mm" />
    <RadzenDatePicker @bind-Value="EventPage.End" ShowTime="true" HourFormat="12" Culture='CultureInfo.CreateSpecificCulture("en-GB")' DateFormat="yyyy/MM/dd hh:mm" />
    <RadzenButton ButtonType="ButtonType.Submit" Text="Create Event" />
</RadzenTemplateForm>

@code {
    public EventPage EventPage = new();
    IEnumerable<User> entertainers;

    protected override void OnInitialized()
    {
        CultureInfo provider = CultureInfo.CreateSpecificCulture("en-GB");

        entertainers = new List<User>
        {
            new User { Id = Guid.NewGuid(), FirstName = "John", LastName = "Doe" },
            new User { Id = Guid.NewGuid(), FirstName = "Jane", LastName = "Smith" },
            new User { Id = Guid.NewGuid(), FirstName = "Bob", LastName = "Brown" },
            new User { Id = Guid.NewGuid(), FirstName = "Alice", LastName = "White" },
            new User { Id = Guid.NewGuid(), FirstName = "Charlie", LastName = "Green" },
            new User { Id = Guid.NewGuid(), FirstName = "David", LastName = "Black" },
            new User { Id = Guid.NewGuid(), FirstName = "Eva", LastName = "Blue" },
            new User { Id = Guid.NewGuid(), FirstName = "Frank", LastName = "Gray" },
            new User { Id = Guid.NewGuid(), FirstName = "Grace", LastName = "Red" },
            new User { Id = Guid.NewGuid(), FirstName = "Harry", LastName = "Yellow" }
        };

        EventPage.EntertainerId = Guid.NewGuid();
    }

    private async Task SubmitEvent(EventPage evt)
    {
        try
        {
            evt.EntertainerId = Guid.Parse("cc232ead-0b61-4395-9b14-a1de097c0e49");
            evt.CafeOwnerId = Guid.Parse("2d22883c-aed7-4725-89fb-1457ddac2882");
         /*   CultureInfo provider = CultureInfo.CreateSpecificCulture("en-GB");
            string format = "dd/MM/yyyy hh:mm";
            DateTime date = DateTime.Now;
            // evt.Date = DateTime.ParseExact(evt.Date.ToShortTimeString(), format,provider);
            // evt.Date = DateTime.Parse( evt.Date.ToString(), CultureInfo.CreateSpecificCulture("fr-FR") );*/
            var response = await HttpClient.PostAsJsonAsync("http://localhost:8080/api/events", evt);
            if (response.IsSuccessStatusCode)
            {
                ShowSuccessNotification();
                ResetForm();
            }
            else
            {
                ShowErrorNotification("An error occurred while processing your request.");
            }
        }
        catch (HttpRequestException)
        {
            ShowErrorNotification("Server is down or there's an issue with the request.");
        }
        catch (Exception ex)
        {
            ShowErrorNotification($"An unexpected error occurred: {ex.Message}");
        }
    }

    private void ShowSuccessNotification()
    {
        NotificationService.Notify(new NotificationMessage { Severity = NotificationSeverity.Success, Summary = "Event created", Detail = "Success Detail", Duration = 4000 });
    }
    
    private void ShowErrorNotification(string message)
    {
        NotificationService.Notify(new NotificationMessage { Severity = NotificationSeverity.Error, Summary = message, Detail = "Unable to create the event", Duration = 4000 });
    }

    private void ResetForm()
    {
<<<<<<< Updated upstream
        Event = new ();
        //Event.EntertainerId = entertainers.First().Id;
=======
        EventPage = new();
    //Event.EntertainerId = entertainers.First().Id;
>>>>>>> Stashed changes
    }
}
